import { Form } from "./_components/Form";

export const metadata = {
    title: "New Forum",
};

export default function NewForumPage() {
    return (
        <>
            <Form />
        </>
    );
}
